import  os, sys

n=input()
w=n.lower()
Alphabet = 'abcdefghijklmnopqrstuvwxyz'
brr = [0]*26

count=0
for ch in w:
    if ch in Alphabet:
        idx=Alphabet.find(ch)
        brr[idx]+=1

for i in range(0,26):
    print(Alphabet[i],":",brr[i])
        
