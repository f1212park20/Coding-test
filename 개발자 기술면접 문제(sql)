최댓값 구하기
SELECT MAX(DATETIME)
FROM ANIMAL_INS
최솟값 구하기
SELECT  MIN(DATETIME)
FROM ANIMAL_INS
동물 수 구하기
SELECT COUNT(ANIMAL_ID) count
FROM  ANIMAL_INS
중복 제거하기
SELECT COUNT(DISTINCT NAME)
FROM ANIMAL_INS

 
고양이와 개는 몇 마리 있을까
SELECT ANIMAL_TYPE, COUNT(*) AS COUNT FROM ANIMAL_INS GROUP BY ANIMAL_TYPE ORDER BY ANIMAL_TYPE
동명 동물 수 찾기
SELECT NAME,COUNT(*) COUNT FROM ANIMAL_INS WHERE NAME IS NOT NULL GROUP BY NAME HAVING COUNT > 1 ORDER BY NAME
입양 시각 구하기(1)
SELECT HOUR(DATETIME) , COUNT(*) COUNT
FROM ANIMAL_OUTS
WHERE HOUR(DATETIME) >= 9 AND HOUR(DATETIME) < 20
GROUP BY HOUR(DATETIME)
ORDER BY HOUR(DATETIME)
입양 시각 구하기(2)
SET @HOUR = -1; SELECT (@HOUR := @HOUR +1) AS HOUR, (SELECT COUNT(*) FROM ANIMAL_OUTS WHERE HOUR(DATETIME) = @HOUR) AS COUNT FROM ANIMAL_OUTS WHERE @HOUR < 23





